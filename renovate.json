{
  "extends": [
    "config:base"
  ],
  "enabledManagers": [
    "regex",
    "github-actions"
  ],
  "labels": ["r:{{depName}}", "r:{{depName}}/{{newVersion}}"],
  "regexManagers": [
    {
      "fileMatch": ["^import_map.json$"],
      "matchStrings": [
        "https://deno.land/x/(?<depName>.+)@(?<currentValue>.+)"
      ],
      "currentValueTemplate": "{{currentValue}}",
      "datasourceTemplate": "github-releases"
    }
  ]
}
